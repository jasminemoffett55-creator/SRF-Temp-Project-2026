library(tidyverse)
library(janitor)
library(lubridate)
library(dplyr)
library(slider)

master2 <- readRDS("C:/Users/eliza.pease/Desktop/temp_data/data/SRF_dataset5.rds") %>%
  clean_names()%>%
  dplyr:: select(timestamp, site, temperature) %>%
  mutate(site = str_trim(site), #get rid of spacing inconsistencies in location names
         datetime = as.Date(timestamp)
        ) 

weir <- master2 %>%
  filter(site == "Weir")



daily_temp_max <- weir %>% #creates dataframe with max temps 
  group_by(datetime) %>% #split data into groups based on unique combo of loc/date
  summarise(max_temp = max(temperature, na.rm=TRUE), #take all temp readings for one day and ignore missing values within the day, compute avg and save it in new col 
            .groups = "drop")


daily_temp_max <- daily_temp_max %>%
  reframe(
    datetime = seq(min(datetime), max(datetime), by = "day")
  ) %>%
  # Join the actual temp data back in
  left_join(daily_temp_max, by = c("datetime")) %>%
  # Calculate rolling average within each site's specific range
  arrange(datetime) %>%
  mutate(
    rolling_7d_mean = slide_index_dbl(
      max_temp,
      datetime, 
      mean, 
      .before= days(6),
      .complete = FALSE,
      na.rm= TRUE
    )
  ) %>%
  ungroup()



MWMT_by_yr_filtered <- daily_temp_max %>% 
  filter(month(datetime) %in% 6:10) %>%
  mutate(year = year(datetime)) %>%
  group_by(year) %>%
  slice_max(rolling_7d_mean, n = 1, with_ties = FALSE) %>% 
  mutate(week = isoweek(datetime)) %>% 
  select(year, datetime, week, MWMT_val = rolling_7d_mean) %>%
  ungroup()






